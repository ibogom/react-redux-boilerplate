import React from 'react'
import { main, wrapper } from './root.scss';
const Root = () => (
    <div className={main}>
        <div className={wrapper}>
            <h1> Welcome to the Boilerplate Website! </h1>
        </div>
    </div>
);

export default Root;
